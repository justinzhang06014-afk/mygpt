version: '3.8'

services:
  # 🚀 官方原生 Hermes Agent 核心
  hermes-core:
    image: nousresearch/hermes-agent:latest
    container_name: independent_hermes_core
    restart: unless-stopped
    environment:
      # 🔓 使用標準憑證路徑，堵住內建安全看門狗的嘴
      - CURL_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt
      - REQUESTS_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt
      - SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt
      - PYTHONHTTPSVERIFY=0

      # 🧠 精準填入您的群聯大腦變數
      - LLM_BASE_URL=https://ainexus.phison.com/api/external/v1
      - LLM_MODEL=Qwen/Qwen3.6-35B-A3B-FP8
      - AINX_API_KEY=AINX-F78D2FCD53915EE37BD0871392FFBFFAF648C53C388950FC6A1F2ED8C534B249
      - AINEXUS_TOKEN=AINX-F78D2FCD53915EE37BD0871392FFBFFAF648C53C388950FC6A1F2ED8C534B249
      - OPENAI_API_KEY=AINX-F78D2FCD53915EE37BD0871392FFBFFAF648C53C388950FC6A1F2ED8C534B249

      # 🔓 啟用對外 Web API 通道
      - API_SERVER_HOST=0.0.0.0
      - API_SERVER_PORT=8642
      - API_SERVER_KEY=my-local-test-token
      - GATEWAY_ALLOW_ALL_USERS=true
    ports:
      # 🔑 兩邊全部鎖定 8642：外面 Windows 實體主機看見 8642，內部核心也跑 8642
      - "8642:8642"
    volumes:
      - ./hermes_data:/opt/data
    command: gateway run

  # 🐍 獨立的 Python 測試執行器
  python-tester:
    image: python:3.10-slim
    container_name: independent_python_tester
    volumes:
      - .:/app
    working_dir: /app
    command: sh -c "pip install requests && python app.py"
    depends_on:
      - hermes-core
# version: '3.8'

# services:
#   # 🚀 官方原生 Hermes Agent 核心
#   hermes-core:
#     image: nousresearch/hermes-agent:latest
#     container_name: independent_hermes_core
#     restart: unless-stopped
#     environment:
#       # 🧠 【大腦換軌】全面切換為未被公司攔截的 Hugging Face 官方相容端點
#       - LLM_PROVIDER=custom
#       - LLM_BASE_URL=https://api-inference.huggingface.co/v1
#       - LLM_MODEL=Qwen/Qwen2.5-7B-Instruct
      
#       # 🔑 填入您剛才複製的 Hugging Face 讀取 Token
#       - 這裡已經刪除hugging face token api 權杖

#       # 🔓 啟用對外 Web API 通道（鎖定 8642 通行碼）
#       - API_SERVER_HOST=0.0.0.0
#       - API_SERVER_PORT=8642
#       - API_SERVER_KEY=my-local-test-token
#       - GATEWAY_ALLOW_ALL_USERS=true
#     ports:
#       # 🔑 兩邊完美鎖死 8642
#       - "8642:8642"
#     volumes:
#       - ./hermes_data:/opt/data
#     command: gateway run

#   # 🐍 獨立的 Python 測試執行器
#   python-tester:
#     image: python:3.10-slim
#     container_name: independent_python_tester
#     volumes:
#       - .:/app
#     working_dir: /app
#     command: sh -c "pip install requests && python app.py"
#     depends_on:
#       - hermes-core
